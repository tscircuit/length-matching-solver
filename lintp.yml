lintp:
  config:
    .dir:
      rule: 'count(filter(children("*"), endsWith($item, ".ts") || endsWith($item, ".tsx"))) <= 10'
      message: "WARNING: A folder contains more than 10 TypeScript files."
  ignore:
    - .git
    - coverage
    - dist
    - node_modules
