/**
 * Internal centerline defaults for the first differential-pair release.
 * These are solver integration values and intentionally are not JSX props.
 */
export const DEFAULT_MIN_DIFFERENTIAL_PAIR_CENTERLINE_SPACING_MM = 0.2

export const DEFAULT_MAX_DIFFERENTIAL_PAIR_CENTERLINE_SPACING_MM = 0.4

export const DIFFERENTIAL_PAIR_GEOMETRY_EPSILON_MM = 1e-7
